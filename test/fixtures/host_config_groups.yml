---
one:
  host: one (Host::Base)
  config_group: one

two:
  host: one (Host::Base)
  config_group: two

three:
  host: common (Hostgroup)
  config_group: one

four:
  host: common (Hostgroup)
  config_group: three

five:
  host_id: 1
  host_type: 'Hostgroup'
  config_group: one

six:
  host: inherited (Hostgroup)
  config_group: two
