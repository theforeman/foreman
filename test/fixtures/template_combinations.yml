---
one:
  provisioning_template: mystring
  hostgroup: common
  environment: production

two:
  provisioning_template: mystring2
  hostgroup: inherited

three:
  provisioning_template: myfinish
  hostgroup: common

four:
  provisioning_template: mystring2
  hostgroup: common
  environment: production

five:
  provisioning_template: myscript
  hostgroup: common
  environment: production
