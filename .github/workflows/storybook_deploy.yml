name: Deploy Storybook Test

on:
  pull_request:
    # paths:
      # - '**.stories.js'
    # types: opened

jobs:
  deploy_storybook:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
      with:
        clean: false
    - name: Run npm install
      run: npm install
    - name: Build Storybook
      run: npm run build-storybook --output=.
    - name: Commit files
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GH Pages Storybook Bot"
        git commit -m "Storybook build" -a
    - name: Push changes
      uses: ad-m/github-push-action@master
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        force: true
        branch: gh-pages
