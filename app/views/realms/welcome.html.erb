<% content_for(:title, _("Realms")) %>
<% description = _("Foreman supports automatically creating realm entries for new hosts. When a realm is selected for a host, Foreman contacts the relevant realm smart proxy to create an entry for the host and retrieve it's one-time registration password.") %>
<% action = { :title => _("Create Realm"), :url => new_realm_path } if authorized_for(:action => :new)%>
<%= react_component('EmptyState', {
                                    :icon => 'key',
                                    :iconType => 'fa',
                                    :header => _('Realms'),
                                    :description => description,
                                    :documentation => { :url => documentation_url("4.3.8Realm") },
                                    :action => action
                                  }) %>
