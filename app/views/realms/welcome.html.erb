<% title_actions link_to(_("New Realm"), new_realm_path) %>
<% title _("Realm Configuration") %>
<div id="welcome">
  <p>
  <%= (_("%{name} supports automatically creating realm entries for new hosts.  When a realm is selected for a host, %{name} contacts the relevant realm %{smart_proxy_name} to create an entry for the host and retrieve it's one-time registration password.") % {:name => SETTINGS[:name], :smart_proxy_name => SETTINGS[:smart_proxy_name]}).html_safe %>
  </p>
</div>
