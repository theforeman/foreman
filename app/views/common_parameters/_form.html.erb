<%
  props = {
    id: @common_parameter.id,
    name: @common_parameter.name,
    keyType: @common_parameter.key_type,
    value: "#{@common_parameter.value_before_type_cast}",
    isMasked: @common_parameter.hidden_value?,
    isNew: @common_parameter.new_record?
  }
%>
<%= react_component('CommonParameterForm', props) %>
