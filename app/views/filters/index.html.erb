<% title _("Filters") %>

<% title_actions link_to(_("New filter"), new_filter_path(:role_id => @role)) %>

<table class="table table-bordered table-striped">
  <thead>
  <tr>
    <% unless params[:role_id] %>
        <th><%= sort :role, :as => _("Role") %></th>
    <% end %>
    <th><%= sort :search, :as => s_("Filter|Search") %></th>
    <th><%= sort :search, :as => s_("Filter|Unlimited") %></th>
    <th></th>
  </tr>
  </thead>
  <tbody>
  <% for filter in @filters %>
      <tr>
        <% unless params[:role_id] %>
            <td>
              <%= content_tag('span', link_to_if_authorized(filter.role.name, hash_for_roles_path(:search => "name = #{filter.role.name}"))) %>
            </td>
        <% end %>
        <td>
          <%= content_tag('span', link_to_if_authorized(filter.search || '-',
                                                        :action => 'edit',
                                                        :id => filter)) %>
        </td>
        <td><%= checked_icon filter.unlimited? %></td>
        <td>
          <%= action_buttons(
                      display_link_if_authorized(_("Edit"), hash_for_edit_filter_path(:id => filter)),
                      display_delete_if_authorized(hash_for_filter_path(:id => filter),
                                                                        :confirm => (_("Delete filter?")))
              )
          %>
        </td>
      </tr>
  <% end %>
  </tbody>
</table>
<%= page_entries_info @filters %>
<%= will_paginate     @filters %>
