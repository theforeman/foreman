<%= fields_for item do |f| %>
  <%= select_f f, :architecture_id, Architecture.all, :id, :to_label, {:include_blank => true}, {:onchange => 'architecture_selected(this);'} %>

  <span id="host_os">
    <span id="os_select">
      <%= render 'common/os_selection/architecture', :item => item %>
    </span>

    <span id="media_select">
      <%= render 'common/os_selection/operatingsystem', :item => item %>
    </span>

  </span>
<% end -%>