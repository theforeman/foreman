<% title "Edit #{@taxonomy}" %>

<%= title_actions(link_to("Fix Mismatches", import_mismatches_taxonomy_path(@taxonomy), :class => 'btn btn-success', :method => :post)) unless @taxonomy.valid? %>
<%= render 'taxonomies/form' , :taxonomy => @taxonomy %>
