<div class="blank-slate-pf">
  <div class="blank-slate-pf-icon">
    <%= icon_text("globe", "", :kind => "fa") %>
  </div>
  <h1><%= _('User groups') %></h1>
  <p>
    <%= _("User groups allows you to add users into a group with roles.") %></br>
    <%= _("User groups can be associated with roles, enabling users to log into Foreman and be automatically granted permissions.").html_safe %></br>
    <%= _("A Foreman user group can be associated to a group stored in an LDAP server, so membership of the LDAP group automatically adds a user to the Foreman user group.").html_safe %></br>
  </p>
  <div class="blank-slate-pf-main-action">
    <%= new_link(_("Create User group"), {}, { :class => "btn-lg" }) %>
  </div>
</div>
