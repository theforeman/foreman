<div class="blank-slate-pf">
  <div class="blank-slate-pf-icon">
    <%= icon_text("globe", "", :kind => "fa") %>
  </div>
  <h1><%= _('Smart Proxy Pools') %></h1>
  <p>
    <%= _("Foreman use Smart Proxy Pools to configure Hosts to communicate with a Proxy with.") %></br>
    <%= _("Generally you would have 1 Pool per Smart Proxy, but if you have a Smart Proxy serving multiple networks with multiple interfaces or a Smart Proxy Cluster
    then you will probably want more than 1 Pool per Smart Proxy or 1 Pool shared between 2 Smart Proxies.").html_safe %></br>
  </p>
  <div class="blank-slate-pf-main-action">
      <%= new_link(_("Create Smart Proxy Pool"), {}, { :class => "btn-lg" }) %>
  </div>
</div>
