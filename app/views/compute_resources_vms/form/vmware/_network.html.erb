<% networks = compute_resource.networks %>
<%= select_f f, :type, compute_resource.nictypes, :first, :last, { },
                 :class => "col-md-2 vmware_type",
                 :label => _('NIC type')
%>
<%= select_f f, :network, networks, :name, :name, { },
                 :class => "col-md-2 vmware_network",
                 :label => _('Network')
%>
