<%#
kind: PXELinux
name: Kickstart oVirt-RHVH PXELinux
model: ProvisioningTemplate
oses:
- oVirt
- RHVH
description: |
  The template to render PXELinux bootloader configuration for oVirt and Red Hat Virtualization Host distributions.
  The output is deployed on the host's subnet TFTP proxy.
-%>
DEFAULT rhvh

LABEL rhvh
KERNEL <%= @kernel %>
APPEND initrd=<%= @initrd %> inst.ks=<%= foreman_url("provision") %> inst.stage2=<%= medium_uri %> local_boot_trigger=<%= foreman_url("built") %> intel_iommu=on
IPAPPEND 2
