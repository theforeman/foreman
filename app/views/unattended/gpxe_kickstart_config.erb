#!gpxe
kernel <%= "#{@system.url_for_boot(:kernel)}" %> ks=<%= foreman_url("provision")%>?static=yes ksdevice=<%= @system.mac %> network kssendmac ip=${net0/ip} netmask=${net0/netmask} gateway=${net0/gateway} dns=${net0/dns}
initrd <%= "#{@system.url_for_boot(:initrd)}" %>

boot
