<%= render 'selected_systems', :systems => @systems %>

<%= form_tag submit_multiple_build_systems_path({:system_ids => params[:system_ids]}) do %>
  <span class="label label-info"><%= _('Provision') %></span>
  <%= _('these systems for a build operation on next boot') %>
<% end %>
