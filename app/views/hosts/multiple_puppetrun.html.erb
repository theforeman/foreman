<%= render 'selected_systems', :systems => @systems %>

<%= form_tag update_multiple_puppetrun_systems_path({:system_ids => params[:system_ids]}) do %>
  <small><%= _('Run Puppet on the following systems') %></small>
<% end %>
