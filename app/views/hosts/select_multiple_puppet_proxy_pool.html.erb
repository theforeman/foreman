<%= render 'selected_hosts', :hosts => @hosts %>

<%= form_for :proxy_pool, :url => update_multiple_puppet_proxy_pool_hosts_path(:host_ids => params[:host_ids]) do |f| %>
  <%= multiple_proxy_pool_select(f, 'Puppet') %>
<% end %>
