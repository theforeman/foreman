<% unless (@clone_host && @clone_host.name)
     title(_("%s Host") % params[:action].capitalize)
   else
     title(_("Clone System %s") % @clone_host.name)
   end  %>

<%= render :partial => 'form' %>
