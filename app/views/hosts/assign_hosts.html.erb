<% title(_("Assign Systems to %s") % @taxonomy.name) %>

<%= render 'hosts/assign_hosts', :hosts => @hosts,
                                 :taxonomy => @taxonomy,
                                 :header => @title %>
