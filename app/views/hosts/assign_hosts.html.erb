<% title "Assign Hosts to #{@taxonomy.name}" %>

<%= render 'hosts/assign_hosts', :hosts => @hosts,
                                 :taxonomy => @taxonomy,
                                 :header => @title %>
