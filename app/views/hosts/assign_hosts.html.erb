<% title(_("Assign Systems to %s") % @taxonomy.name) %>

<%= render 'systems/assign_systems', :systems => @systems,
                                 :taxonomy => @taxonomy,
                                 :header => @title %>
