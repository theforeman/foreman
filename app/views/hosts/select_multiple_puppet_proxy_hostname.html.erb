<%= render 'selected_hosts', :hosts => @hosts %>

<%= form_for :proxy_hostname, :url => update_multiple_puppet_proxy_hostname_hosts_path(:host_ids => params[:host_ids]) do |f| %>
  <%= multiple_proxy_hostname_select(f, 'Puppet') %>
<% end %>
