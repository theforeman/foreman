<% title(_("All Puppet Classes for %s") % @system) %>
  <table class="table table-bordered table-striped">
    <tr>
      <th>Class</th>
    </tr>
    <% @system.classes_from_storeconfigs.each do |klass| %>
      <tr>
        <td><%= h klass %></td>
      </tr>
    <% end %>
  </table>
<%= link_to 'back', system_path(@system) %>
