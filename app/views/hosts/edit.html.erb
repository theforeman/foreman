<% title(_("Edit %s") % @system) %>
<% if SETTINGS[:unattended] %>
  <% title_actions display_link_if_authorized((@system.managed? ? _("Unmanage system") : _("Manage system")),
    hash_for_toggle_manage_system_path(:id => @system.name), {:method => :put}) %>
<% end %>

<%= render :partial => 'systems/form' %>
