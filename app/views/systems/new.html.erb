<% unless (@clone_system && @clone_system.name)
     title(_("%s System") % params[:action].capitalize)
   else
     title(_("Clone System %s") % @clone_system.name)
   end  %>

<%= render :partial => 'form' %>
