<% if authorized? %>
  <% title_actions multiple_actions_select, (link_to_if_authorized _("New System"), hash_for_new_system_path) %>
<% end %>
<%= render 'list', :systems => @systems, :header => @title || _("Systems")  %>
